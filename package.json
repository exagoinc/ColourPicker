{
	"name": "colourpicker",
	"version": "0.0.1",
	"description": "A pure TypeScript colour picker",
	"main": "colourpicker.js",
	"scripts": {
		"build": "tsc && less-watch-compiler --run-once src build",
		"dev": "concurrently --kill-others \"tsc -w\" \"less-watch-compiler --source-map src build\" \"http-server\""
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/volleio/ColourPicker.git"
	},
	"author": "Lucas Volle",
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/volleio/ColourPicker/issues"
	},
	"homepage": "https://github.com/volleio/ColourPicker#readme",
	"devDependencies": {
		"concurrently": "^3.6.1",
		"less-watch-compiler": "^1.13.0",
		"tslint": "^5.16.0",
		"tslint-config-airbnb-base": "^0.1.0"
	},
	"dependencies": {
		"less": "^3.9.0",
		"typescript": "^2.9.2"
	}
}
